BACKEND_URL=http://127.0.0.1:8000

# Optional: set this only if the browser should call the backend directly instead
# of using the same-origin /api/backend proxy.
# NEXT_PUBLIC_BACKEND_URL=http://127.0.0.1:8000
